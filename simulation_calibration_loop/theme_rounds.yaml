# Placeholder — old flat-schema orchestrator was deleted in the trajectory-branch
# purge (Stage 1.4 of optuna_search_trajectory.md). Stage 3 rewrites this slot as
# `theme_rounds_trajectory.yaml`, so this filename is retained only to reserve
# the path in tools that hard-code it. Do not add configs here; author them in
# the new orchestrator instead.
rounds: 0
common: {}
configs: []
