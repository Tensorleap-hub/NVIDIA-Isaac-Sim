"""Generate the cosmos_v3 configs — the Cosmos-profile twin of base_v3.

Per optimization-recipe.md ("Two temporal densities"), the OD/training set and
the Cosmos set must share an IDENTICAL scene/camera style and differ ONLY in
timing + capture.video. To guarantee that, this script does not re-declare any
style: it loads each base_v3 config (the OD/training set) and applies just the
Cosmos-profile deltas:

    run.num_frames : 10   -> 128       (a real video clip)
    agent.capture_dt: <base_v3>  -> 0.0333   (30 fps smooth playback)
    capture.video  : false -> true     (MP4 out -> CosmosWriter -> augmentation)
    run.data_dir   -> .../cosmos_v3/<name>

Everything else (uniform scatter, counts, start_mode, min_path_m, bounds, FOV,
height, lighting, characters, distractors, materials) is inherited verbatim from
base_v3. Regenerate base_v3 first, then run this.

Regenerate:  python _generate_cosmos_v3.py
"""

import copy
from pathlib import Path

import yaml

HERE = Path(__file__).resolve().parent
BASE_V3 = HERE.parent / "base_v3"

COSMOS_FPS_DT = 0.0333   # 30 fps
COSMOS_NUM_FRAMES = 128

HEADER = (
    "# cosmos_v3 — Cosmos-profile twin of base_v3 (dense-in-time video).\n"
    "# AUTO-GENERATED by _generate_cosmos_v3.py from the base_v3 config of the\n"
    "# same name. IDENTICAL scene/camera style; only num_frames + capture_dt +\n"
    "# capture.video differ (see optimization-recipe.md 'Two temporal densities').\n"
    "# Edit base_v3 (or this generator), never this file by hand.\n"
)


def main() -> None:
    srcs = sorted(BASE_V3.glob("exp*.yaml"))
    if not srcs:
        raise SystemExit(f"no base_v3 configs found in {BASE_V3}; generate base_v3 first")
    for src in srcs:
        cfg = yaml.safe_load(src.read_text())
        cfg = copy.deepcopy(cfg)
        cfg.setdefault("run", {})["num_frames"] = COSMOS_NUM_FRAMES
        cfg.setdefault("agent", {})["capture_dt"] = COSMOS_FPS_DT
        cfg.setdefault("capture", {})["video"] = True
        cfg["run"]["data_dir"] = f"/isaac-sim/palletjack_sdg/palletjack_data/trajectory/cosmos_v3/{src.stem}"
        out = HERE / src.name
        out.write_text(HEADER + yaml.safe_dump(cfg, sort_keys=False, default_flow_style=False))
        print(f"wrote {out.relative_to(HERE.parent.parent.parent)}")
    print(f"\n{len(srcs)} cosmos_v3 configs generated (128f @ 30fps, video on).")


if __name__ == "__main__":
    main()
