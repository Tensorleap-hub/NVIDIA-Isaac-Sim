"""Generate the cosmos_optuna configs — Cosmos-profile clips of the weekend
Optuna (DINOv2 theme-rounds) winners.

Sources the top-4 trials by objective (best_top10.yaml) from
simulation_calibration_loop/promoted_baseline_trajectory_dino/ (final study =
occurrence_r03) and applies the exact cosmos_v4 profile deltas on top of each
trial's full SDG config (see cosmos_v4/_generate_cosmos_v4.py for the rationale
of every override):

    run.num_frames  : -> 128       (a real video clip)
    agent.capture_dt: -> 0.0333    (30 fps smooth playback)
    capture.video   : -> true      (MP4 out -> CosmosWriter -> augmentation)
    trajectory.short_path_fill: {enabled: true, min_spacing_m: 0.05, max_passes: 2}
                                   (one-way smooth motion, not OD-set ping-pong)
    run.data_dir    : -> .../cosmos_optuna/<name>  (runner overrides per seed)

Everything else — including each trial's own environment, bounds_xy, camera
mount/intrinsics, lighting, palette, distractor occurrence — is inherited
verbatim from what the trial actually rendered during the search, so the video
is a faithful dense-in-time twin of the scored still set.

Trial selection (MMD vs real, lower better; baseline 0.4056) — top 4 by
objective, all beat the baseline:
    r1  iter006_run002  0.3260  full_warehouse  (promoted best)
    r2  iter009_run001  0.4054  full_warehouse
    r3  iter004_run003  0.4163  full_warehouse
    r4  iter002_run002  0.4421  full_warehouse
"""

from pathlib import Path

import yaml

HERE = Path(__file__).resolve().parent
EXPORTS = HERE.parents[3] / "simulation_calibration_loop" / "promoted_baseline_trajectory_dino"

COSMOS_FPS_DT = 0.0333   # 30 fps
COSMOS_NUM_FRAMES = 128

# (export file, run_id) -> emitted config name
PICKS = [
    ("best_top10.yaml", "iter006_run002", "r1_top1_full_warehouse"),
    ("best_top10.yaml", "iter009_run001", "r2_top2_full_warehouse"),
    ("best_top10.yaml", "iter004_run003", "r3_top3_full_warehouse"),
    ("best_top10.yaml", "iter002_run002", "r4_top4_full_warehouse"),
]

HEADER_TMPL = (
    "# cosmos_optuna — Cosmos-profile clip of Optuna trial {run_id}\n"
    "# (weekend DINO theme-rounds, occurrence_r03 study; MMD={mmd:.4f}, env={env}).\n"
    "# AUTO-GENERATED by _generate_cosmos_optuna.py from\n"
    "# promoted_baseline_trajectory_dino/{src}. Scene identical to the scored\n"
    "# trial; only num_frames + capture_dt + capture.video + short_path_fill differ.\n"
    "# Edit the generator, never this file by hand.\n"
)


def main() -> None:
    exports = {}
    for src, run_id, name in PICKS:
        if src not in exports:
            exports[src] = yaml.safe_load((EXPORTS / src).read_text())
        trials = {t["run_id"]: t for t in exports[src]["trials"]}
        if run_id not in trials:
            raise SystemExit(f"{run_id} not found in {src} (snapshot overwritten by a newer study?)")
        trial = trials[run_id]
        cfg = trial["config"]

        cfg.setdefault("run", {})["num_frames"] = COSMOS_NUM_FRAMES
        cfg.setdefault("agent", {})["capture_dt"] = COSMOS_FPS_DT
        cfg.setdefault("capture", {})["video"] = True
        spf = cfg.setdefault("trajectory", {}).setdefault("short_path_fill", {})
        spf["enabled"] = True
        spf["min_spacing_m"] = 0.05
        spf["max_passes"] = 2
        cfg["run"]["data_dir"] = f"/isaac-sim/palletjack_sdg/palletjack_data/trajectory/cosmos_optuna/{name}"

        header = HEADER_TMPL.format(
            run_id=run_id, mmd=trial["objective_value"],
            env=cfg.get("environment", {}).get("name"), src=src,
        )
        out = HERE / f"{name}.yaml"
        out.write_text(header + yaml.safe_dump(cfg, sort_keys=False, default_flow_style=False))
        print(f"wrote {out.name}  (MMD={trial['objective_value']:.4f}, env={cfg.get('environment', {}).get('name')})")
    print(f"\n{len(PICKS)} cosmos_optuna configs generated (128f @ 30fps, video on).")


if __name__ == "__main__":
    main()
